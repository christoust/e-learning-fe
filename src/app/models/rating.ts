export class Rating {
    courseId: number;
    feedback: string;
    rating: number;
    userName: string;
    date: string; // Assuming the date is stored as a string in the format "yyyy-MM-dd"
  }
  